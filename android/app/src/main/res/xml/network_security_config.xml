<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP/WS) traffic globally -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Optionally, you can define specific domains if needed -->
    <!--
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">13.201.33.113</domain>
    </domain-config>
    -->
</network-security-config>
